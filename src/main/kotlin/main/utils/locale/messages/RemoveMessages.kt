package main.utils.locale.messages

import main.utils.locale.LocaleMessage

enum class RemoveMessages : LocaleMessage {
    REMOVE_INVALID_ID,
    REMOVED,
    REMOVED_LOG,
    COULDNT_REMOVE,
    COULDNT_REMOVE_NAME
}