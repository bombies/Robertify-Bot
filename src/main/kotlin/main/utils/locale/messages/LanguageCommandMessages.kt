package main.utils.locale.messages

import main.utils.locale.LocaleMessage

enum class LanguageCommandMessages : LocaleMessage {
    LANGUAGE_EMBED_DESC,
    LANGUAGE_SELECT_MENU_PLACE_HOLDER,
    LANGUAGE_CHANGED
}