package main.utils.locale.messages

import main.utils.locale.LocaleMessage

enum class ClearQueueMessages : LocaleMessage {
    CQ_NOTHING_IN_QUEUE,
    DJ_PERMS_NEEDED,
    QUEUE_CLEARED_USER,
    QUEUE_CLEAR
}