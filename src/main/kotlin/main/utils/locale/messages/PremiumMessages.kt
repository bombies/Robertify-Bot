package main.utils.locale.messages

import main.utils.locale.LocaleMessage

enum class PremiumMessages : LocaleMessage {
    LOCKED_COMMAND_EMBED_TITLE,
    LOCKED_COMMAND_EMBED_DESC,
    NOT_PREMIUM,
    IS_PREMIUM
}